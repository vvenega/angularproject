import { ProductRequested } from './productrequested.model';

describe('ProductRequested', () => {
  it('should create an instance', () => {
    expect(new ProductRequested()).toBeTruthy();
  });
});
