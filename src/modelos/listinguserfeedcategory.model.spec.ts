import { ListingUserFeedCategory } from './listinguserfeedcategory.model';

describe('ListingUserFeedCategory', () => {
  it('should create an instance', () => {
    expect(new ListingUserFeedCategory()).toBeTruthy();
  });
});
