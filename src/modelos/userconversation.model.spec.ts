import { UserConversation } from './userconversation.model';

describe('UserConversation', () => {
  it('should create an instance', () => {
    expect(new UserConversation()).toBeTruthy();
  });
});
